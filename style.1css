:root {
    --bg-color: #121826; /* Dark Navy (Not pure black) */
    --gold: #d4af37;
    --text-color: #ffffff;
    --border-style: 2px solid #d4af37;
    --card-bg: rgba(255, 255, 255, 0.05);
}

[data-theme="light"] {
    --bg-color: #fdfaf5; /* Creamy White */
    --text-color: #1a1a1a;
    --gold: #8b6e23;
    --card-bg: #ffffff;
}

body {
    background-color: var(--bg-color);
    color: var(--text-color);
    font-family: 'Playfair Display', serif;
    margin: 0;
    padding: 20px;
    transition: 0.5s;
}

.outer-border {
    border: var(--border-style);
    padding: 20px;
    min-height: 95vh;
    position: relative;
    box-shadow: inset 0 0 50px rgba(0,0,0,0.5);
}

/* Navbar & Dropdown */
.navbar {
    display: flex;
    justify-content: center;
    gap: 40px;
    border-bottom: 1px solid var(--gold);
    padding-bottom: 15px;
}

.nav-links {
    list-style: none;
    display: flex;
    gap: 25px;
}

.nav-links a {
    text-decoration: none;
    color: var(--text-color);
    font-weight: bold;
    font-size: 14px;
    letter-spacing: 2px;
}

.dropdown-menu {
    display: none;
    position: absolute;
    background: var(--bg-color);
    border: 1px solid var(--gold);
    padding: 10px;
    list-style: none;
    z-index: 100;
}

.dropdown:hover .dropdown-menu { display: block; }

.dropdown-menu li a { font-size: 12px; display: block; padding: 5px; }

/* Header */
.main-header { text-align: center; margin-top: 40px; }
.logo-text { font-family: 'Cinzel', serif; font-size: 50px; letter-spacing: 10px; margin-bottom: 0; }
.tagline { font-style: italic; color: var(--gold); }

/* Car Showcase */
.car-showcase {
    display: flex;
    justify-content: space-around;
    margin: 50px 0;
    border-top: 1px solid var(--gold);
    border-bottom: 1px solid var(--gold);
    padding: 40px 0;
}

.car-item img {
    width: 250px;
    filter: drop-shadow(0 0 10px rgba(0,0,0,0.5));
    border-radius: 5px;
}

.car-item p { text-align: center; margin-top: 15px; color: var(--gold); }

/* Grid Boxes */
.info-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-top: 40px;
}

.info-box {
    border: 1px solid var(--gold);
    padding: 10px;
    text-align: center;
    background: var(--card-bg);
}

.info-box img { width: 100%; height: 150px; object-fit: cover; }

.mode-btn {
    background: transparent;
    border: 1px solid var(--gold);
    color: var(--gold);
    padding: 5px 15px;
    cursor: pointer;
}

